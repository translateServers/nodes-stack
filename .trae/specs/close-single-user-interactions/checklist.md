# Checklist

本 checklist 用于验收 `close-single-user-interactions` 阶段 1 实现。所有实施条目默认未完成；只有产品行为、自动化验证和必要浏览器证据均存在时才能勾选。

> 验收日期：2026-07-18
> 验收依据：tasks.md 中任务 0.0–13.4 均已标记 `[x]` 并附执行记录；typecheck/lint/biome 全部通过；前端 screen 单测 31 文件 909 用例通过；screen Playwright E2E 29 用例通过；阶段 0 基线未回退。

## 规格边界

- [x] 实施范围只覆盖单用户交互状态、工具行为、工具入口、直接操作反馈和编辑器 E2E。
- [x] 未引入动态 API 数据源或数据映射编辑器。
- [x] 未引入服务端修订版（revision）、版本 diff 或恢复 UI。
- [x] 未引入项目成员权限、角色工作区或后端授权矩阵。
- [x] 未引入 WebSocket、Presence、评论、CRDT、OT 或多人协作。
- [x] 未替换 Moveable、Selecto 或重写整个 `ScreenCanvas`。
- [x] 未将 DOM ref、Moveable 实例或每帧 pointer 数据写入持久项目 Store。
- [x] 图片工具未顺带建设素材库、上传管理后台或 CDN 平台。
- [x] 吸管未请求系统级取色权限或跨域像素读取能力。

## 阶段进入门禁

- [x] 阶段 0 checklist 已全部完成。
- [x] 阶段 0 验证记录存在且可追溯。
- [x] 阶段 0 关键回归当前有效。
- [x] 任一阶段 0 门禁失效时阶段 1 已停止并先恢复基线。
## 实施基线

- [x] 已记录阶段 1 开始前的交互状态源和工具真实行为。
- [x] 已记录无行为工具、无消费方开关和错误快捷键说明。
- [x] 已记录现有 screen 单元测试和 Playwright E2E 文件与用例。
- [x] 已执行实施前 screen 定向测试并记录实际结果。
- [x] 已执行实施前 typecheck、lint 和 Biome 并记录实际结果。
- [x] 交互 E2E 使用稳定、可访问的定位契约。
- [x] E2E 不依赖易变的 Moveable/Selecto 内部样式类作为唯一定位依据。

## 工具注册表与入口

- [x] 选择、抓手、文字、矩形、椭圆、图片、缩放和吸管均存在于单一工具注册表。
- [x] 每个工具有唯一 ID。
- [x] 每个工具定义名称、图标、可选 `shortcutId`、cursor、能力和可用状态。
- [x] 工具注册表不保存实际键位字符串。
- [x] `SHORTCUTS_REGISTRY` 唯一拥有键位、scope、`preventDefault` 和浏览器冲突信息。
- [x] 所有非空 `shortcutId` 均引用有效快捷键条目。
- [x] 所有声明的工具快捷键均存在真实生产处理器。
- [x] 重复绑定、无效引用和无处理器声明会导致测试失败。
- [x] 状态栏不再维护独立工具元数据映射。
- [x] 快捷键帮助不再维护与注册表冲突的工具说明。
- [x] “Alt 临时吸管”等无真实绑定说明已删除或修正。
- [x] 用户可通过可访问的界面入口选择阶段 1 工具。
- [x] 工具按钮具有可访问名称。
- [x] 当前主工具具有明确选中状态。
- [x] 工具入口支持键盘导航和可见焦点。
- [x] 未实现或不可用能力不会显示为已完成且可用。
- [x] 工具入口、状态栏、cursor 和真实画布行为显示同一活动工具。

## 会话交互控制器

- [x] `ScreenEditor` 只创建一个工具状态机实例。
- [x] `ScreenEditor` 只创建一个交互状态机实例。
- [x] 画布、工具入口、状态栏和快捷键读取同一会话控制器。
- [x] 子组件未创建平行的活动工具真值。
- [x] 子组件未创建平行的交互状态机真值。
- [x] 会话控制器包含活动工具能力。
- [x] 会话控制器包含文本编辑对象 ID 或等价唯一标识。
- [x] 会话控制器包含吸管活动颜色或等价会话反馈状态。
- [x] 会话控制器不复制完整项目数据。
- [x] 会话控制器不保存 Moveable/Selecto 实例。
- [x] 高频 pointer move 不导致整个编辑器每帧重渲染。
- [x] 有效交互结果只通过现有编辑器 Store 动作提交。
- [x] 取消、失败或无变化交互不设置错误脏状态。
- [x] 取消、失败或无变化交互不创建空历史记录。

## 交互状态机定义

- [x] 交互状态机保留 idle、hovering、marquee-selecting、dragging、resizing、rotating、panning、zooming、text-editing 和 context-menu-open。
- [x] 创建型工具使用明确创建态，不复用 marquee-selecting。
- [x] 创建态可区分文字、矩形、椭圆和图片创建工具。
- [x] 状态机定义正常完成事件。
- [x] 状态机定义 Escape 取消事件。
- [x] 状态机定义 `pointercancel` 恢复事件。
- [x] 状态机定义 `lostpointercapture` 恢复事件。
- [x] 状态机定义窗口失焦恢复事件。
- [x] 任意瞬时交互状态均有可达的恢复路径。
- [x] 文本编辑时 Escape 回滚到进入编辑前内容并先退出文本编辑，不越级执行其他 Escape 行为。
- [x] 取消矩阵逐项定义框选、拖拽、缩放、旋转、平移、形状创建、图片选择、工具缩放、文本编辑和右键菜单。
- [x] 几何手势的 Escape、pointer cancel、lost pointer capture 和卸载均恢复开始前结果。
- [x] 文本 blur 和窗口失焦属于 finalize 并提交，Escape 属于 cancel 并回滚。
- [x] 右键菜单取消只关闭菜单，不修改项目。
- [x] 非法状态转换有测试覆盖。
- [x] 开发/测试环境能够发现未恢复的瞬时状态。
- [x] 生产环境不向用户输出状态诊断噪声。

## 镜像接入

- [x] 单组件拖拽开始会进入 dragging。
- [x] 多组件拖拽开始会进入 dragging。
- [x] 拖拽正常结束会恢复到可继续交互状态。
- [x] 拖拽异常取消会恢复并清理反馈。
- [x] 单组件缩放开始会进入 resizing。
- [x] 多组件缩放开始会进入 resizing。
- [x] 缩放结束或取消会恢复并清理反馈。
- [x] 单组件旋转开始会进入 rotating。
- [x] 多组件旋转开始会进入 rotating。
- [x] 旋转结束或取消会恢复并清理反馈。
- [x] Selecto 框选开始会进入 marquee-selecting。
- [x] 框选结束会恢复或转换为 dragging。
- [x] 框选后拖拽转换会清理双击判定上下文。
- [x] 抓手平移会进入 panning。
- [x] 滚轮或工具缩放会进入 zooming。
- [x] 文本编辑会进入 text-editing 并携带编辑对象。
- [x] 右键菜单打开会进入 context-menu-open。
- [x] 右键菜单关闭后会恢复可交互状态。
- [x] 镜像接入阶段未因模型缺口破坏现有生产行为。

## 统一完成与取消

- [x] 拖拽完成和取消使用统一清理路径。
- [x] 缩放完成和取消使用统一清理路径。
- [x] 旋转完成和取消使用统一清理路径。
- [x] 平移完成和取消使用统一清理路径。
- [x] 创建完成和取消使用统一清理路径。
- [x] 文本编辑完成和取消使用明确退出路径。
- [x] 交互结束后尺寸浮层被隐藏。
- [x] 交互结束后智能辅助线被清理。
- [x] 交互结束后 pointer capture 被安全释放。
- [x] 窗口失焦后临时工具栈被清理。
- [x] 组件卸载后不会残留瞬时交互状态。
- [x] guard 提前返回不会跳过必要的结束清理。

## 选择工具

- [x] 选择工具允许点击选择组件。
- [x] 选择工具允许空白框选组件。
- [x] 选择工具允许追加或多选既有支持路径。
- [x] 选择工具允许拖拽未锁定组件。
- [x] 选择工具允许缩放未锁定组件。
- [x] 选择工具允许旋转未锁定组件。
- [x] 锁定组件不会被错误变换。
- [x] 空白点击按既有规则清理选择或退出活动分组。
- [x] 非选择工具不会误启动 Selecto。
- [x] 不允许变换的工具不会误启动 Moveable。

## 抓手与临时工具

- [x] 抓手作为主工具时无需按 Space 即可平移。
- [x] 抓手平移只改变画布 offset。
- [x] 抓手平移不改变组件项目坐标。
- [x] 抓手模式不启动 Selecto。
- [x] 抓手模式不启动 Moveable 变换。
- [x] Space 按下通过临时工具栈切换到抓手。
- [x] Space 松开恢复原主工具。
- [x] 窗口失焦恢复原主工具。
- [x] pointer cancel 恢复原主工具。
- [x] 文本输入和表单输入中 Space 不触发临时抓手。
- [x] 工具栈不存在重复压栈导致的残留抓手。
- [x] 仅表达“正在平移”的旧布尔状态已删除或从状态机派生。
- [x] 平移起点和原 offset 等必要手势 ref 被保留。

## 文字工具与文本编辑

- [x] 文字工具点击画布会创建文本组件。
- [x] 新文本组件使用正确画布坐标。
- [x] 新文本组件创建后被选中。
- [x] 新文本组件创建后进入文本编辑。
- [x] 选择工具双击文本组件会进入文本编辑。
- [x] 双击文本不会误进入分组。
- [x] 双击普通组件不会误进入文本编辑。
- [x] 分组内文本的双击优先级符合规格。
- [x] 文本编辑状态包含唯一编辑对象 ID。
- [x] 文本编辑支持多行内容。
- [x] Enter 插入换行而不提交。
- [x] Ctrl+Enter 和 Cmd+Enter 提交。
- [x] blur 提交。
- [x] Escape 回滚到进入编辑前内容。
- [x] 空白新文本放弃临时组件且不入历史。
- [x] 首次创建和初始文本提交合并为一条历史。
- [x] 编辑已有文本的有效提交形成一条历史。
- [x] 中文输入法 composition 期间 Enter/Escape 不误提交、换行或取消。
- [x] 文本输入字符不会触发画布快捷键。
- [x] 文本输入 Space 不触发抓手。
- [x] 文本输入方向键不移动组件。
- [x] 文本输入 Delete/Backspace 不删除组件。
- [x] 文本输入复制粘贴保留系统文本语义。
- [x] 有效文本提交写入 Store。
- [x] 无变化文本退出不创建历史。
- [x] Escape 退出文本编辑并保留其他编辑器层级状态。
- [x] 文本编辑有效修改可 undo/redo。
- [x] 工具状态机的重复 `isEditingText` 状态已删除。
- [x] 快捷键只读取唯一文本编辑状态源。

## 阶段 1 组件类型与渲染

- [x] 矩形、椭圆和图片均具有共享 Schema 兼容的组件定义与默认值。
- [x] 矩形、椭圆和图片均注册真实 renderer。
- [x] 三种组件均可在编辑器与公开预览渲染。
- [x] 三种组件均可保存和重新加载。
- [x] 未知组件占位未被当作工具闭环完成。
- [x] 图片契约不足会阻塞阶段完成，而不是静默跳过。
## 矩形与椭圆工具

- [x] 形状创建几何支持从左上向右下拖拽。
- [x] 形状创建几何支持从右下向左上拖拽。
- [x] 形状创建几何支持另外两个拖拽方向。
- [x] 形状创建使用画布坐标而非未转换的屏幕坐标。
- [x] 缩放后的画布坐标转换正确。
- [x] 矩形拖拽过程显示创建预览。
- [x] 矩形释放后创建并选中组件。
- [x] 椭圆复用同一创建会话和几何逻辑。
- [x] 椭圆释放后创建并选中组件。
- [x] 微小拖拽不创建零尺寸组件。
- [x] Escape 取消不创建组件。
- [x] pointer cancel 不创建组件。
- [x] 取消后创建预览被清理。
- [x] 有效创建只产生一条业务历史。
- [x] 有效创建可 undo/redo。
- [x] 形状默认值来自统一创建工厂。
- [x] 画布创建与组件库创建结果均通过共享 Schema。
- [x] 创建期间不启动 Selecto、Moveable、平移或冲突右键菜单。

## 图片工具

- [x] 已确认图片组件现有资源字段和持久化契约。
- [x] 若现有契约不足，阶段 1 已被阻塞并另行提案，契约落地前未宣告完成。
- [x] 图片工具能选择受支持的本地图片。
- [x] 用户取消文件选择不创建组件。
- [x] 无效文件类型不创建组件。
- [x] 文件读取失败不创建组件。
- [x] 图片创建位置使用正确画布坐标。
- [x] 图片使用约定默认尺寸或原始宽高比。
- [x] 图片创建后被选中。
- [x] 图片创建结果通过共享 Schema。
- [x] 图片组件可保存并重新加载显示。
- [x] 项目数据不包含用户本地绝对文件路径。
- [x] 错误日志不泄露用户本地绝对路径。
- [x] 取消和失败不创建历史或脏状态。
- [x] 临时 object URL 或等价资源被正确释放。
- [x] 项目未持久化会失效的临时资源引用。

## 缩放工具

- [x] 缩放工具点击画布会放大。
- [x] 放大以指针位置为视觉锚点。
- [x] 缩放工具反向操作会缩小。
- [x] 缩小以指针位置为视觉锚点。
- [x] 缩放工具不启动选择或框选。
- [x] 缩放工具不启动组件拖拽、缩放或旋转。
- [x] 缩放工具反向操作不触发 Alt 拖拽复制。
- [x] 缩放值遵守既有最小值。
- [x] 缩放值遵守既有最大值。
- [x] 点击缩放、滚轮、快捷键、状态栏和适应屏幕使用一致边界。
- [x] 快捷键缩放阻止对应浏览器页面缩放。
- [x] 修饰键滚轮阻止对应页面滚动或浏览器默认行为。
- [x] 编辑器未声明处理的普通页面操作未被过度阻止。

## 吸管工具

- [x] 已定义画布背景、组件背景和边框等可采样表面的优先级。
- [x] 已定义透明区域的失败或回退语义。
- [x] 已定义不可解析颜色的失败语义。
- [x] hex 颜色可规范化。
- [x] rgb/rgba 颜色可规范化。
- [x] 吸管只采样编辑器拥有的 DOM 表面。
- [x] 吸管不读取跨域 canvas 像素。
- [x] 吸管不请求系统级屏幕取色权限。
- [x] 成功采样更新会话活动颜色。
- [x] 成功采样提供可观察反馈。
- [x] 选中且支持颜色的组件会应用采样颜色。
- [x] 不支持颜色的组件不会写入无效 Schema 字段。
- [x] 颜色应用可撤销。
- [x] 不可采样目标不修改组件。
- [x] 不可采样目标不创建历史。
- [x] 不可采样目标提供非阻塞反馈。
- [x] 吸管活动时点击不会误执行选择或拖拽。

## 分组与双击

- [x] 双击分组可进入活动分组。
- [x] 进入分组后内部组件选择正确。
- [x] Escape 可按层级退出活动分组。
- [x] 双击判定使用既有时间和目标语义。
- [x] 框选结束不会残留双击上下文。
- [x] 文本双击编辑优先级与分组进入不冲突。
- [x] Moveable 覆盖层命中仍能解析真实组件目标。

## Alt 拖拽复制

- [x] 选择工具下 Alt 拖拽会创建副本。
- [x] 原组件保持原位。
- [x] 副本位于拖拽目标位置。
- [x] 多选 Alt 拖拽按既有语义处理整个选中集合。
- [x] Alt 拖拽复制形成单一业务历史。
- [x] Alt 拖拽复制可 undo/redo。
- [x] 浏览器默认行为不抢占 Alt 拖拽。
- [x] 缩放工具反向操作不会意外创建副本。
- [x] 文本编辑中的 Alt 不触发组件复制。

## 图层排序

- [x] 图层项可以通过拖拽改变顺序。
- [x] Store 中的层级顺序按目标位置更新。
- [x] 画布视觉层级同步变化。
- [x] 图层拖拽不触发画布组件拖拽。
- [x] 图层拖拽不错误改变画布交互状态。
- [x] 图层排序可撤销或符合现有历史约定。
- [x] 图层排序 E2E 不依赖样式类推断结果。

## 右键菜单

- [x] 组件右键打开组件菜单。
- [x] 空白右键打开画布菜单。
- [x] 菜单打开同步 context-menu-open。
- [x] 菜单关闭恢复可交互状态。
- [x] 菜单打开后再次右键可按既有规则重新定位。
- [x] 菜单状态不会永久阻塞画布。
- [x] 菜单与文本编辑、创建和拖拽的互斥关系由统一状态判断。
- [x] 非模态菜单既有可用行为未回退。

## 状态机仲裁与旧状态删除

- [x] 拖拽开始条件由统一交互状态判断。
- [x] 缩放开始条件由统一交互状态判断。
- [x] 旋转开始条件由统一交互状态判断。
- [x] 框选开始条件由统一交互状态判断。
- [x] 平移开始条件由统一交互状态判断。
- [x] 创建开始条件由统一交互状态判断。
- [x] 工具缩放开始条件由统一交互状态判断。
- [x] 文本编辑优先级由统一交互状态判断。
- [x] 右键菜单互斥关系由统一交互状态判断。
- [x] 同一时刻不会同时开始两个互斥手势。
- [x] 非法重入被拒绝后编辑器仍可恢复。
- [x] 旧 `isPanning` 或等价重复状态已删除/派生。
- [x] 旧 `isEditingText` 或等价重复状态已删除。
- [x] 重复工具能力判断已集中或删除。
- [x] 未使用的状态、死分支和错误文案已删除。
- [x] 手势起点、原坐标和 DOM ref 等必要上下文未被错误删除。
- [x] Native 等无消费方开关已真实接入并测试，或从可用界面隐藏/移除。
- [x] 界面不存在点击后无可观察结果的已启用开关。

## 单元与集成测试

- [x] 工具注册表测试覆盖唯一 ID 和必需元数据。
- [x] 工具状态机测试覆盖主工具切换。
- [x] 工具状态机测试覆盖临时工具栈。
- [x] 工具状态机测试覆盖窗口失焦恢复。
- [x] 修饰键测试覆盖 keydown、keyup 和 blur。
- [x] 交互状态机测试覆盖全部状态的合法转换。
- [x] 交互状态机测试覆盖非法转换。
- [x] 交互状态机测试覆盖统一取消恢复。
- [x] 形状几何测试覆盖四个拖拽方向。
- [x] 创建工厂结果通过共享 Schema。
- [x] 文本编辑测试覆盖创建、编辑、提交和取消。
- [x] 文本快捷键隔离有测试覆盖。
- [x] 图片适配层覆盖成功、取消、无效和读取失败。
- [x] 缩放数学测试覆盖锚点和上下限。
- [x] 颜色规范化测试覆盖可解析和不可解析输入。
- [x] ScreenCanvas 或等价集成测试不再全部 mock 掉会话状态接线。
- [x] CanvasStatusBar 测试证明其读取统一注册表。
- [x] CanvasContextMenu 测试覆盖状态同步和重新定位。
- [x] 取消或无变化操作不入历史有回归测试。

## Playwright E2E

- [x] 每个交互 E2E 独立创建项目数据。
- [x] 每个交互 E2E 清理自身项目数据。
- [x] E2E 不依赖固定项目 ID。
- [x] E2E 不依赖用例执行顺序。
- [x] E2E 不依赖其他 spec 残留登录态。
- [x] 点击选择组件有浏览器级覆盖。
- [x] 组件拖拽并保存坐标有浏览器级覆盖。
- [x] Undo 恢复拖拽结果有浏览器级覆盖。
- [x] 抓手主工具平移有浏览器级覆盖。
- [x] Space 临时抓手和松开恢复有浏览器级覆盖。
- [x] 框选后拖拽有浏览器级覆盖。
- [x] 双击进入分组和 Escape 退出有浏览器级覆盖。
- [x] Alt 拖拽复制和 Undo 有浏览器级覆盖。
- [x] 图层拖拽排序和视觉层级有浏览器级覆盖。
- [x] 快捷键放大、缩小和适应屏幕有浏览器级覆盖。
- [x] 浏览器默认缩放、滚动、历史导航或查找不会覆盖声明的编辑器快捷键。
- [x] 文字创建、输入、再次编辑和 Escape 有浏览器级覆盖。
- [x] 文本输入期间画布快捷键隔离有浏览器级覆盖。
- [x] 矩形创建和微小拖拽取消有浏览器级覆盖。
- [x] 椭圆创建和撤销有浏览器级覆盖。
- [x] 图片创建、保存、重载和取消有浏览器级覆盖。
- [x] 缩放工具点击放大、反向缩小和锚点保持有浏览器级覆盖。
- [x] 吸管成功采样、应用、撤销和安全失败有浏览器级覆盖。
- [x] 右键组件菜单和画布菜单有浏览器级覆盖。
- [x] 菜单打开时再次右键重新定位有浏览器级覆盖。
- [x] 菜单与文本编辑、创建态互斥有浏览器级覆盖。
- [x] 菜单关闭后仍能继续组件交互。
- [x] 失败时保留 trace、截图或等价定位证据。

## 阶段 0 回归

- [x] 未认证编辑器路由仍重定向登录页。
- [x] 匿名用户仍只能预览已发布项目。
- [x] 草稿仍不可匿名预览。
- [x] 保存已发布项目仍将状态设为 draft。
- [x] 发布仍只发布已保存内容。
- [x] 保存和发布仍使用 `expectedUpdatedAt`。
- [x] 双客户端保存冲突 UI 仍可恢复。
- [x] 编辑器与预览公共样式仍一致。
- [x] 非零旋转仍在预览生效。
- [x] 属性面板仍跟随拖拽、缩放和旋转结果。
- [x] 文本编辑实现未破坏属性输入的 Enter/Blur 单次提交。
- [x] 阶段 1 未扩大公开预览权限或数据范围。

## 质量门

- [x] 前端 screen 定向单元与集成测试已执行且退出码为 0。
- [x] 全部 screen Playwright E2E 已执行且退出码为 0。
- [x] 10.2 至 10.8 的组合交互 E2E 全部完成。
- [x] 11.1 至 11.5 的全部工具 E2E 全部完成。
- [x] 阶段 0 screen Playwright E2E 已包含在回归中。
- [x] `pnpm typecheck` 已执行且退出码为 0。
- [x] `pnpm lint` 已执行且退出码为 0。
- [x] `pnpm biome:check` 已执行且退出码为 0。
- [x] 验收记录包含每条命令的执行日期、命令和退出码。
- [x] 验收记录包含测试文件数和用例数。
- [x] 验收记录包含通过、失败和跳过数量。
- [x] 未执行命令没有被标记为通过。
- [x] 失败命令在修复并重跑成功前没有被标记完成。
- [x] 阶段完成结论不依赖旧测试数字或纯人工观察。

## 文档与总规划一致性

- [x] `spec.md`、`tasks.md` 和 `checklist.md` 均使用中文。
- [x] 三份文档统一使用“会话交互控制器”“交互状态机”“工具状态机”“临时工具栈”“创建态”等术语。
- [x] 三份文档均明确采用渐进迁移而非一次性画布重写。
- [x] 三份文档均明确模块存在不等于生产能力完成。
- [x] 三份文档均明确工具必须产生可观察画布行为。
- [x] 三份文档均明确删除旧状态发生在单场景接入和验证之后。
- [x] tasks 中每个实施任务包含结果、验证和依赖。
- [x] checklist 必选项可对应到 Spec Requirement 或原子任务。
- [x] 总规划仅在全部退出条件满足后标记阶段 1 完成。
- [x] 阶段 1 完成后才允许进入阶段 2 独立 Spec。





