- 所有模型的主键 ID 均使用 String 类型并默认生成 UUID (`@default(uuid())`)。
- 所有业务实体模型均包含 `isActive`、`createdAt` 和 `updatedAt` 审计字段，其中 `updatedAt` 使用 `@updatedAt` 自动更新。
- 所有模型均使用 `@@map` 显式指定数据库表名，通常采用复数形式（如 `users`, `roles`）。
- 软删除或状态控制统一使用 `isActive` Boolean 字段，默认值为 `true`。
