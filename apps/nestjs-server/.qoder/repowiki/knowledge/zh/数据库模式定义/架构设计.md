- 采用模块化 Schema 设计，主文件 `schema.prisma` 配置生成器和数据源（SQLite），具体模型拆分至 `schema/` 子目录下的独立 `.prisma` 文件。
- 定义了五大核心实体：`User`（用户）、`Role`（角色）、`Menu`（菜单）、`Dict`（字典类型与值）、`RefreshToken`（刷新令牌）。
- 通过 `@relation` 属性建立实体间关联，如用户与角色的多对多关系、菜单的自引用层级结构、字典类型与值的级联删除关系。
- 提供 `seed.ts` 脚本用于初始化默认管理员账户，集成 `bcryptjs` 进行密码哈希处理。