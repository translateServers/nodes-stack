- 所有业务逻辑异常必须抛出 `BusinessException` 并携带 `BizCode`，由全局过滤器统一转换为标准 API 错误响应格式。
- 控制器返回的数据通过 `TransformInterceptor` 自动包装为包含 `code`、`message` 和可选 `data` 字段的统一响应结构。
- 受保护的路由默认需要 JWT 认证，仅当使用 `@Public()` 装饰器标记时才会跳过 `JwtAuthGuard` 的校验逻辑。
- 配置项访问强制使用 `TypedConfigService` 的点语法或命名空间方法，确保运行时配置的类型安全与存在性校验。