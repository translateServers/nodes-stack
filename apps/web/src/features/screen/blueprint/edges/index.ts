/**
 * 蓝图边组件模块入口（任务 4.3）
 *
 * 公开 API：
 * - ExecEdge：执行流边统一样式（贝塞尔曲线 + 方向箭头 + 选中态高亮）
 * - 类型：ExecEdge（React Flow Edge 实例类型）
 *
 * 引脚磁吸与兼容判定辅助位于 lib/pin-compatibility.ts。
 */

export { ExecEdge } from './exec-edge';
