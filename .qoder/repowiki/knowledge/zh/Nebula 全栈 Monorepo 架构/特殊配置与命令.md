- **开发启动**：根目录执行 `pnpm dev` 并行启动前后端，或使用 `pnpm dev:web` / `pnpm dev:server` 单独启动。
- **数据库同步**：在 `apps/nestjs-server` 下执行 `prisma db push` 同步 SQLite 结构。
- **代码规范**：通过 `pnpm format` (Prettier) 和 `pnpm lint` (ESLint) 维持多包一致性。